          <div class="box">
            <h2>Skills</h2>
	    <div class="skills">
		    <% getlist(Skill).each {|sk| %>
			    <div class="item-skills hidden-print" data-percent="<%= sk.level %>"><%= sk.name %></div>
		    <% } %>
<div class="visible-print"><% getlist(Skill).each{|srv| %><%= srv.name %> | <% } %> </div>
              <div class="skills-legend clearfix hidden-print">
                <div class="legend-left legend">Beginner</div>
                <div class="legend-left legend"><span>Proficient</span></div>
                <div class="legend-right legend"><span>Expert</span></div>
                <div class="legend-right legend">Master</div>
              </div>
            </div>
          </div>